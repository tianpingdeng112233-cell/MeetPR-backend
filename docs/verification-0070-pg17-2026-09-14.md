# 0070 迁移与 PR #276 收货验证

> 历史验收快照；2026-09-16 的实际备份、0070 与部署状态见 [上线记录](deployment-045-web101-53-2026-09-16.md)。

2026-09-14 首轮收货，配套 iOS #340。本节验证范围为本机隔离 PostgreSQL 17 与合并后的工作树；首轮没有连接 RDS、执行远端迁移或部署。

同日登录后已完成 RDS 只读预检与隔离 PG18 补验，见 [阿里云预检](aliyun-preflight-2026-09-14.md)。线上迁移与部署仍未执行。

## 代码与合并

- 实装分支：`feat/045-coach-plan-shift`，原 head `76bc696175ba803618980464f578afd3d0260961`。
- 合入目标：`origin/staging` 的 `42238a99812c75bb92f8bd24bdc74134d0074d3e`。
- 唯一冲突：`src/config.ts` 的 `Config` 类型。保留后移发布开关与 Google 多 audience 两边的可选 DI 字段，不改变各自解析行为。
- 将目标 `origin/staging` 合入实装分支的 merge commit 为 `a4d03ac`；验收文档与状态记录提交为 `6d0c056`，两者已推送。PR #276 尚未合并到远端 staging；本记录不表示远端 CI 或部署完成。

## PostgreSQL 17 实跑

- 镜像：`postgres:17`，实际版本 `17.10 (Debian 17.10-1.pgdg13+1)`。
- 镜像 digest：`sha256:ef92240eff6b0bcce8ccf038c2edcd0d8fd8ef90621849993b8c8995881ab09a`。
- 使用一次性 `--network none` 容器，无宿主端口、无业务凭证；通过 `docker exec -i ... psql -U postgres -v ON_ERROR_STOP=1 -X` 执行。
- 使用合成数据及最小前置表 `users`、`plans`、`plan_days`、0070 前的 `plan_day_shifts`，保留迁移依赖的 UUID、DATE、TIMESTAMPTZ、FK 与 UNIQUE 约束。
- 直接执行原始 `db/migrations/0070-plan-shift-batches.sql`，没有替换 BIGSERIAL、相关 UPDATE 或 setval。
- SQL SHA-256：`bef6f9b0e6dadda0ff9dd426937c074c4bb464fafd2776e21d04e7bf5dae234c`。

| 场景                     | 观察与 SQL ASSERT 结果                                                                                                            |
| ------------------------ | --------------------------------------------------------------------------------------------------------------------------------- |
| 空表迁移                 | 全事务成功，UPDATE 0、INSERT 0；空表 setval 返回 NULL，没有报错                                                                   |
| 空表迁移后的旧写者       | 省略 `seq`、`created_at`，只插天级行；首个 `seq=1`，无父批次也可写入                                                              |
| 存量回填                 | 两个批次共四行，插入顺序为 4、2、3、1；含同一 created_at 的两行；迁移后按 `(created_at,id)` 对应 `seq=1,2,3,4`                    |
| 批次元数据回填           | 恰好两行，actor 为学员、role 为 `coached_student`、offset 为 1；anchor 分别为 2026-05-01、2026-05-02，created_at 为该批最早行时间 |
| 存量回填后的首个 nextval | 旧写者省略 seq 并写入更早 created_at，返回 `seq=5`；不依赖时间排序                                                                |
| CHECK 与索引             | 非法 actor_role、offset=31 均触发 check_violation；两个新增索引存在                                                               |
| 级联清理                 | 删除合成 plan 后，父批次与天级行均为 0                                                                                            |

上述验证覆盖 PR 指定的空表、存量表和回填后首个 nextval 门禁；合成小数据不代表真实库迁移耗时或已完成线上数据审计。

## 工程门禁

- 合并前：`pnpm install --frozen-lockfile`、typecheck、lint 通过，153 个测试文件、1148 个用例通过。
- 合并后：typecheck、lint、format:check、test、build 全部通过；153 个测试文件、1163 个用例通过。
- `git diff --cached --check` 通过；没有未解决冲突。
- 本机测试日志：`/tmp/meetpr-backend-045-baseline-test.log`、`/tmp/meetpr-backend-045-merged-test.log`；格式日志：`/tmp/meetpr-backend-045-format.log`。

## 部署交接

1. 按 `db/MIGRATIONS-APPLIED.md` 先完成备份并确认非零文件，再对获准的 staging 数据库应用 0070，记录实际结果。0069 是其他 PR 占号，0070 不依赖它，不自动补跑其他未合迁移。
2. `COACH_PLAN_SHIFT_ENABLED=false` 时部署新镜像并确认全部实例完成切换；确认镜像已进入 registry 后再 dispatch 部署 workflow。
3. 确认无旧实例后把开关置 true 并重启，验证 coach POST/DELETE、学员 GET 推荐日期与撤销闭环。
4. 最后放开 plan-web #101 入口并核验实际网页 bundle；推送还需现有 APNs 配置和真机证据。
5. 记录部署 SHA、workflow、health/API 与迁移结果；F-001 contract 迁移仍单独处理，先 reconciliation 再加 FK。

回退须遵循 spec 045 的混跑限制：coach 开启前可关门保留 expand schema；开启并产生 coach 批次后，不能直接把旧 reader 镜像当作已验证回退方案。保留新镜像并先关闭教练写入口，另行评估回退。
