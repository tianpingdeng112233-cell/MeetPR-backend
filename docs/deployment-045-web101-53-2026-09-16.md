# 045 + Web 多日编辑上线记录

## 已验证的备份与迁移

- 目标：杭州 `meetpr-rds-v01-staging` / `pgm-bp1h7t65b7if01rq`，数据库 `meetpr`，PostgreSQL 18.3。
- 原线上镜像 `42238a99812c75bb92f8bd24bdc74134d0074d3e`，功能开关未设置，默认关闭。
- 迁移前执行仓库 `scripts/backup-db.ts` 的 TypeScript 转译产物；临时 PostgreSQL 18.6 客户端来自官方 PGDG，依赖来自 Debian / 阿里云 Debian 镜像，下载逐个 SHA-256 校验。工具只解包到旧容器 `/tmp`。
- 全库逻辑备份：`/tmp/meetpr-ops0070/backups/meetpr-development-2026-09-16T080158.038Z.sql.gz`，1,013,344 bytes，`gzip -t` 通过；SHA-256 `b4bb1fce91e08496e0389fb5031dffa7fb820f80645ffab18bce1ec11dbe9715`。文件名 development 来自现有运行环境字段，不代表连接了另一数据库。
- SAE 文件上传/下载接口均返回 `InvalidParameter.Obviously: fail to exec`，逻辑备份未导出；该容器临时文件不作为持久恢复依据。
- 持久恢复依据：RDS 手动全量实例快照任务 `t-0mqie3hpxlx0zw6o6u`，控制台 2026-09-16 09:05:45 开始、09:08:32 结束，执行成功 100%（原样记录控制台显示时间）。确认成功后才执行迁移。
- 执行未经改写的 `db/migrations/0070-plan-shift-batches.sql`，SHA-256 `bef6f9b0e6dadda0ff9dd426937c074c4bb464fafd2776e21d04e7bf5dae234c`。原事务外设置 `lock_timeout=5s`、`statement_timeout=60s`，失败回滚。
- 0070 前：7 条天级行、2 批，表和 seq 列均不存在，历史批次的 plan/student 唯一性通过。
- 0070 后：天级行仍 7 条，父批次 2 行，seq 无空值、7 个唯一值，max(seq)=sequence last_value=7。输出 `MIGRATION_0070_VERIFIED`。
- 0069 与 F-001 contract 迁移不属于本次范围，未执行。

## 镜像与网页

- 后端 #276 已合并至 staging：`d0b919d7bc73979bfcafd370fc791ec6e4428747`。CI run 35070058532、ACR build run 35070058413 均成功。
- 后端部署 [run 35072260580](https://github.com/tianpingdeng112233-cell/MeetPR-backend/actions/runs/35072260580) 成功，health、登录路由及同源 bundle 冒烟通过；原 42238a9 实例已退场，d0b919d 单实例 Running。随后通过 SAE 提交 `COACH_PLAN_SHIFT_ENABLED=true` 并保持原有 29 项环境变量不变；配置滚动后新实例 4vn4p Running，运行值核验与最终 web 换装待完成。
- Web 集成 PR #102：`d71a41311d7bae55dfeb1a18a2cd1d7d2c0c6776`，包含 #101 / #53。810 测试、构建与独立双轴审查通过，详见 web 仓验收记录。

## 回退边界

0070 是 expand 迁移，保留历史数据；上线期间先维持 coach gate 关闭。新镜像全量后才开 gate，再合并与换装 web。产生 coach 批次后，出现问题时先关闭 gate 并保留兼容新数据的后端；不能直接回退到旧 reader。恢复整库快照属于另一次需确认的数据恢复操作。

## 验收边界

APNS_ENV 仍为 sandbox。本次没有修改推送凭证、发送真实学员消息或重放失败 outbox；TestFlight production APNs 的真实送达仍待单独验收。
