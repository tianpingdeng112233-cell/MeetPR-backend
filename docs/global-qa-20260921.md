# Global RN build 22 专用验收账号

Status: Complete（账号预置与 API 登录/绑定；W3 全量验收仍进行中）

David 已授权方案 A：在现有 Global 只新增一个专用教练和一个学员，重名立即停止，不能重置或修改已有账号。2026-09-21 补充授权：新测试账号凭证保存在本机仓库外私有文件，无需 Bitwarden。

本操作分支沿用 spec 039 的教练后台预置入口，将此分支的预置命令收紧为仅创建。工作流限定专用教练邮箱，密码经独立临时 GitHub secret 传入；现有 GLOBAL_COACH_PASSWORD 不改动。数据库连接与 CA 留在工作流内。完成后删除本次临时 secret。

测试 seam：预置命令调用的公开 provisionCoach 函数，与 Kysely/数据库边界；验证已有邮箱（含大小写）拒绝且密码、身份不变，新账号的 email identity 和密码可用。执行前独立 Standards / Spec 审查。

验收：预置成功；教练、学员各自经 Global email login 成功；仅两者之间建立绑定；本地凭证目录 0700、文件 0600；记录无密码的 HTTP/工作流证据。

Out of Scope：发布后端、数据库迁移、修改其他用户、密码重置、邮件发送、PR 合并、海外上架。此操作分支不合并，不替换默认分支现有预置语义。

## 执行前收货

- Standards：独立 reviewer CLEAN；Spec：独立 reviewer CLEAN。均只读检查上述四文件，各自复跑三项预置测试通过。
- 基线 153 suites / 1163 tests；改后 154 suites / 1166 tests。typecheck、lint、format:check、build 全部通过。
- 测试曾先后观察到缺少公开入口、大小写碰撞未返回明确错误的 red，再实现 green；最终 lint 指出的非空断言已改为显式检查。
- 凭证文件 `/Users/david/.local/share/meetpr/global-qa-20260921/accounts.json`，目录 0700 / 文件 0600；本文件不含密码。建号结果见下。

## 实际执行结果

- 2026-09-21，受审脚本 `f8db2a1` 在 [工作流 35588654243](https://github.com/tianpingdeng112233-cell/MeetPR-backend/actions/runs/35588654243) 返回 `COACH_CREATED`，job success；此后删除独立临时 secret 并复核名称已不存在。未改共享 secret，未部署、迁移或合并。
- 教练 email login 200；学员 email register 201、email login 200。首查教练 roster / invite codes 均为空。
- 为此对账号创建 single_use 邀请码（201），学员申请（201），教练接受并跳过评估期（200），再次读取名单（200）。未写入其他用户的数据。
- Android Global QA 包通过真实教练登录并展示唯一学员 RN22 QA Student。后续 App/训练/视频证据统一在 RN 仓 `docs/verification-w3-2026-09-21.md`，本记录不代表 W3 整体验收完成。
- 密码仅保留于上文本地文件，不进 Git / 对话；token 仅在执行进程内使用。
